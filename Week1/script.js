const getElement = (identifier = "") => document.querySelector(identifier);

function addTask() {
    let comment = getElement("comment").value;
    let kepentingan = getElement("kepentingan").value;
    

}